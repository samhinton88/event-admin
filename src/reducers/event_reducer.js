import {
  EVENT_CREATE,
  EVENT_EDIT,
  EVENT_DELETE,
  FETCH_EVENTS
} from '../actions/types';

const defaultState = {};

export default (state = defaultState, action) => {
  switch (action.type) {
    case EVENT_CREATE:

    case EVENT_EDIT:

    case EVENT_DELETE:

    case FETCH_EVENTS:

  }

  return state;
}
