
const defaultState = {
  headerKeys: ['_id', 'title', 'image', 'venue', 'time', 'text', 'interestedUsers']
}

export default (state = defaultState, action) => {
  switch (action.type) {

  }
  return state;
}
